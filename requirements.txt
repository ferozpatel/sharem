# Delta Exchange India trading bot — Python dependencies
# (subsystem colocated with the existing Fyers/Sensex bot under sharem/)

# HTTP transport for the signed Delta REST client (DeltaSigner) and data feed
requests==2.32.3

# OHLC candle handling and resampling in helper_delta.getHistorical
pandas==2.2.2

# AWS EC2 lifecycle management and optional Secrets Manager credential source
boto3==1.34.144

# ------------------------------------------------------------
# Dev / test dependencies
# ------------------------------------------------------------
# Test runner
pytest==8.2.2

# Property-based testing (min 100 examples per property; see design Testing Strategy)
hypothesis==6.108.5
